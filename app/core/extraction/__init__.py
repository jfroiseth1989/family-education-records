"""Text extraction: turning a stored original into page-level, citable text.

See docs/PHASE_2_PLAN.md §5. Extraction only ever reads a stored original
— see app/core/extraction/service.py's docstring for the integrity
guarantee this package must never violate.
"""
