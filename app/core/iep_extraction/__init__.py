"""IEP Consistency Review: deterministic structured-field extraction and
manual/assisted entry (docs/IEP_CONSISTENCY_REVIEW_PLAN.md §3).

Step 2 scope only: a services extractor (services.py) and the manual
entry path (manual.py), both built on the shared record/field creation
core (records.py). No comparison/flag generation exists yet -- that is
a later step, and it is deliberately kept out of this package until
then.
"""
